#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <map>
#include <vector>

using namespace std;
const int maxn = (int) 2e5+10;
int a[maxn];
int n, q;

void solve() {
}

int main() {
  int T;
  scanf("%d", &T);
  for (int cas = 1; cas <= T; ++ cas) {
    scanf();

    printf("Case #%d: ", cas);
    solve();
  }

  return 0;
}

