#include <stdio.h>
#include <stdlib.h>
#include <string.h>
const int maxn = 100010;
int m,l;
char s[maxn];

void solve() {
  int sl = (int) strlen(s);
  int ans = 0;
  for (int i = 0; sl-i>=m*l; ++ i) {
  }
}

int main() {
  while (scanf("%d%d", &m, &l) != EOF) {
    scanf("%s", s);
    solve();
  }
  return 0;
}
